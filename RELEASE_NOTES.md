# 灰猫地图 1.0.24 更新说明

## 修复内容

- 修复 Android Auto 百度瓦片请求超时、断网或 HTTP 错误时，异常从线程池逃逸导致应用进程崩溃的问题。
- 瓦片下载现在完整捕获连接、响应码、图片解码和连接关闭阶段的异常；失败只记录日志，不影响手机导航和车机服务。
- 增加失败瓦片 3 秒冷却重试，避免网络不稳定时同一瓦片每帧重复请求，降低线程、网络和 CPU 压力。
- 非 2xx 响应和无效图片会作为可恢复失败处理；成功下载后自动清除重试状态。

## 未修改内容

- 未修改手机百度导航 View、路线规划、导航生命周期和定位。
- 未修改 Android Auto 路线显示校准、地图缩放、50 米比例尺和指引同步。
- 未继续处理手机顶部留白、定位偏移和比例尺显示效果。
- 三个微信位置转发插件仅随版本重新构建，转发逻辑未修改。

## 已知问题

- 网络不可用期间尚未缓存的瓦片仍会显示深色背景，但不应再导致应用崩溃。
- Android Auto 车辆与路线显示校准、50 米比例尺在部分车机上仍可能不符合预期，本版按用户要求暂时跳过。
- 手机导航顶部安全区域问题本版未继续处理。

## 安装说明

- 主应用 `versionCode` 为 25，使用固定长期 release 证书，可直接覆盖安装 1.0.23。
- 百度 AK 应绑定包名 `com.huimao.map` 和 Release SHA-1 `DE:FF:00:C2:E1:8A:20:62:9C:4A:17:67:B7:27:A5:08:CC:1B:F0:E4`。
- Release 同时提供百度、高德和腾讯三个微信位置转发插件。
