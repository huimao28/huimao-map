# 灰猫地图 Wear OS 1.1.8

## 修复内容

- Wear OS 端新增手机导航语音播报事件同步。
- 手机每次触发导航播报时，手表进行振动提示。
- 新增后台实时导航通知，显示下一步指令、转向距离和当前道路。
- 增加 Android 13 及以上通知权限申请。
- Wear OS 继续作为手机端灰猫地图的非独立配套应用发布。

## 未修改内容

- Wear OS 包名保持为 `com.huimao.map`。
- 手机端灰猫地图版本保持为 `1.1.7`，本次不发布新的手机端版本。
- Wear OS 版本内部 `versionCode` 按项目原有连续规则递增为 `35`。
- 手机与手表之间继续使用 Google Play services Wearable Data Layer 通信。

## 已知问题

- 需要在手表端允许通知权限，才能显示实时导航通知。
- 手表需要允许振动，且系统勿扰模式可能抑制振动。
- 手机和手表必须完成配对，并使用同一 Google 账号。
- 当前环境无法直接运行 Gradle，最终构建以 GitHub Actions 为准。

## 安装或升级注意事项

- 本版本为 Wear OS `1.1.8`，内部 `versionCode` 为 `35`。
- 手机端仍使用灰猫地图 `1.1.7`，无需升级手机端即可配合测试。
- 通过 Google Play 现有灰猫地图应用的 Wear OS 封闭测试轨道分发。
- 如果 Play Console 要求同一应用的手机端版本同步提升，请先在对应测试轨道中确认 Wear OS 外形规格的发布配置。


## 修复内容

- 修复手机端 Wearable Data Layer 同步实现中的 `DataMap` 构建错误。
- 手机端新增手机—Wear OS 连接桥，自动同步导航状态。
- 手表端接收并显示手机导航指令、转向距离、当前道路和剩余距离。
- 导航开始、导航回调更新、到达目的地和退出导航时都会同步状态。
- 无手表或手表未连接时，手机端导航保持正常运行。
- 保留三个微信位置转发插件：百度、高德、腾讯版本。
- Release 构建同时生成手机 APK、AAB、Wear OS APK 和三个转发插件 APK。

## 未修改内容

- 手机主应用包名仍为 `com.huimao.map`。
- Wear OS 应用包名与手机端保持一致，为 `com.huimao.map`。
- 百度地图 Android AK 配置方式不变。
- 百度导航、路线规划、定位和微信位置转发功能保持原有行为。
- Android Auto 仍处于暂时移除状态。

## 已知问题

- 当前环境无法直接运行 Gradle，本次构建以 GitHub Actions 为最终编译验证。
- Wear OS 通信依赖手机和手表均安装 Google Play services，并完成系统配对。
- 手表实际字体、圆形屏幕边缘裁切和蓝牙连接稳定性仍需真机确认。
- 百度 SDK 仍需要有效的 Android AK，且包名和签名 SHA-1 必须匹配。

## 安装或升级注意事项

- 本版本为 `1.1.7`，手机端和 Wear OS 端的内部 `versionCode` 均为 `34`，与展示版本号独立管理。
- 主应用安装包：`HuimaoMap_1.1.7.apk`。
- 手表安装包：`HuimaoMap_Wear_1.1.7.apk`。
- 微信转发插件分别为百度、高德、腾讯版本；代理 APK 与对应官方地图使用相同包名，不能和对应官方地图同时安装。
- Wear OS 版本现在作为手机端灰猫地图的附属设备版本发布，使用同一 Play 应用条目和包名；手机与手表 versionName 均保持为 `1.1.7`。
- Wear OS 应用通过 Play Console 的 Wear OS 外形规格发布，不应创建独立的 Wear OS 商店应用。
- 如果升级旧腾讯转发代理 1.4，需先卸载旧代理后再安装 1.1.7 版本。
