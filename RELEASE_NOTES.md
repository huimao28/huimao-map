# 灰猫地图 1.1.2 更新说明

## 修复内容

- 修复 1.1.1 启动导航时可能直接“路线规划失败”的问题。
- 修复启动参数里的异常缓存起点被误当成可靠定位基准，导致真实定位回归后被防跳点逻辑过滤的问题。
- 导航启动时现在优先等待独立定位客户端输出第一帧可靠位置，再发起路线规划；等待短时间仍无定位时才使用启动参数兜底。

## 技术调整

- 启动参数中的起点只作为临时种子，不再立即写入百度导航外部定位通道。
- 防跳点基准只来自真实定位回调或已确认可靠的位置，避免跨国/跨省缓存点锁死路线规划。
- `startRoutePlan()` 优先使用最近 10 秒内的可靠定位，降低百度导航 SDK 判定起点无效的概率。

## 保留修复

- 保留 1.1.1 的 Android Auto 加载兜底：百度后台位图为空时显示车机兜底导航图，不再停留加载文字。
- 保留百度后台地图 View 离屏测量/布局修复。
- 保留定位弱/丢失时的 Android Auto 惯性导航状态显示。
- Release 继续同时提供百度、高德和腾讯三个微信位置转发插件。

## 已知问题

- 如果设备完全无法获取定位，应用仍会在 15 秒后提示定位失败或使用启动起点兜底；建议确保系统精确位置权限和定位服务已开启。

## 安装说明

- 主应用版本升级为 `1.1.2`，`versionCode` 为 28，可直接覆盖安装 1.1.1。
- 百度 AK 应绑定包名 `com.huimao.map` 和固定 Release SHA-1：`DE:FF:00:C2:E1:8A:20:62:9C:4A:17:67:B7:27:A5:08:CC:1B:F0:E4`。
