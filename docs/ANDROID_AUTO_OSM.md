# Android Auto OSM 车机实现

Android Auto 只负责显示，手机端百度导航 SDK 负责路线、定位、转向、偏航和语音。

路线渲染采用：车辆投影到路线线段、裁剪已行驶部分、路线描边/主线分层，以及路口箭头按路线距离定位。车机端不根据 OSM 路网重新规划或推断转向。

OSM 底图渲染器与导航状态桥分离，Surface 销毁不会停止手机导航；Surface 恢复后使用最新状态重新绘制。
